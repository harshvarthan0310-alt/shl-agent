services:
  - type: web
    name: shl-agent
    runtime: docker
    plan: free
    envVars:
      - key: Groq_API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
    healthCheckPath: /health
